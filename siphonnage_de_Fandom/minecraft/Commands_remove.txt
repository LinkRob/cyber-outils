{{Exclusive|education}}{{Infobox command
|name=remove
|hidden=1
|oplevel=0
}}

Has the same function as [[Commands/kill|/kill]].

==Syntax==

: <code>remove [target: target]</code>

==Arguments==

<code>target</code>
: Specifies the target to be removed. Must be a [[Commands#Target selectors|target selector]].

==Result==

Fails if the arguments are not specified correctly, or if the executor is not a <code>worldbuilder</code> while targeting other people's agents.

==Examples==

* To remove the executor's agent.
** <code>/remove @c</code>
* To remove all agents.
** <code>/remove @v</code>
* To remove all entities.
** <code>/remove @e</code>

== History ==

{{History|Education}}
{{History||1.7|Added the remove command.}}
{{History|foot}}

== See also ==

:{{cmd|kill}} – kill any entity

{{Commands}}

[[ja:コマンド/remove]]